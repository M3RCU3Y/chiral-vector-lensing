# Generated figures

Run `make figures` to regenerate the corrected numerical plots used by manuscript v11. PNG outputs are intentionally ignored by Git so the repository tracks their generators and numerical inputs rather than duplicate binaries.
