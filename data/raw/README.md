# Generated linear handoff

`linear_handoff_eps1e-3.npz` is deterministic and is intentionally not tracked.
Generate it with:

```bash
make handoff
```

`make test` also generates the file before running the test suite.
